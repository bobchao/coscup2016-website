// Include library
var React     = require('react');

// Implement index page
var TimetableFilter = React.createClass({
    render: function() {
        return (
            <div role="timetable-filter"></div>
        );
    }
});

module.exports = TimetableFilter;
